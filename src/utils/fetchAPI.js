const GetHotel = CITY_NAME => {
  
  // const city = `https://trippy-konexio.herokuapp.com/api/hotels/city/${CITY_NAME}`


  // fetch(city)
  // .then(response => response.json())
  // .then(data => {return data.results})

  
}

export { GetHotel }